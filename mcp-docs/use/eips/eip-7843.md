# EIP-7843 — SLOTNUM opcode

> **Status:** **Planned** — `run_block` can set a header slot; the 7843 catalog row is not runnable yet. **Public MCP not launched yet.** Use the [website exploration](https://feelyourprotocol.org/eip-7843-slotnum-opcode) today.

## What became possible

**How do you read the beacon slot on-chain without hardcoding 12-second slots?** EIP-7843 adds **`SLOTNUM`** (`0x4b`): the consensus layer writes the slot on the execution header, and the opcode pushes it for 2 gas.

Dividing the block timestamp by 12 works only while slots stay 12 seconds. The opcode abstracts that away.

## What you will ask your agent (when the module ships)

Once the public MCP lists 7843 as runnable ([Connect](/use/connect)), try:

- *“What does SLOTNUM push if the header slot is 42?”*
- *“Same program on Osaka vs Amsterdam — does 0x4b succeed?”*
- *“Why not TIMESTAMP ÷ 12 for the current slot?”*
- *“Here’s bytecode — what’s the stack top on slot 12345678?”*

Until then, use the [website exploration](https://feelyourprotocol.org/eip-7843-slotnum-opcode) for the interactive textbook.

## What the server will do

| Step | Action | Purpose |
| --- | --- | --- |
| 1 | [Discover support](/use/tools/describe-capabilities) | Confirm when 7843 is listed as runnable |
| 2 | [Run block](/use/tools/run-block) with a header slot | Per-tx result / return data equals the slot on Amsterdam; `slotNumber` rejected on Osaka |

**Today:** [Run block](/use/tools/run-block) already accepts `header.slotNumber` on Amsterdam. [Run bytecode](/use/tools/run-bytecode) still uses a mock header (slot 0). The named 7843 module is the next slice — do not treat a lab example as the catalog row.

## Twins and spec

| | Link |
| --- | --- |
| **Website exploration** (interactive textbook) | [EIP-7843 on feelyourprotocol.org](https://feelyourprotocol.org/eip-7843-slotnum-opcode) |
| **Canonical spec** | [EIP-7843](https://eips.ethereum.org/EIPS/eip-7843) |
| **Coverage index** | [Coverage](/use/coverage) |

## Changelog

<Changelog
  title="EIP-7843 MCP Catalogue Changelog"
  :entries="[
    { version: 'v0.2', date: '2026-09-10', summary: 'run_block ships header.slotNumber; 7843 catalog row still Planned until the module.' },
    { version: 'v0.1', date: '2026-09-10', summary: 'Planned twin — exploration live; runnable catalog after run_block / header slot.' },
  ]"
/>
