# EIP-8024 — Stack opcodes short

First Feel Your Protocol short-form video. Overlay copy in `content.json`, timeline in `playbook.json`.

```bash
# from website/
npm run website:build
npm run video:record -- eip-8024 --dry-run    # inspect steps
npm run video:record -- eip-8024 --preview    # fast half-res render
npm run video:record -- eip-8024              # full 1080×1920 .webm
```

Output: `output/eip-8024-<timestamp>.webm`
