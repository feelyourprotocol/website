/**
 * Layout tokens for exploration pages. Keep static — do not size from measured DOM.
 *
 * Cover-only right column (`rightPanel` unset): cap `image.webp` to roughly a
 * typical left `.exploration-c` on desktop. Companion panels use a smaller
 * per-exploration `imageBoxHeight` (16rem–19rem) instead.
 */
export const COVER_COLUMN_IMAGE_HEIGHT = '48rem'
