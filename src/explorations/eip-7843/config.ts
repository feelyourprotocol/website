import type { BytecodeStepperConfig } from '@/eComponents/bytecodeStepperEC/types'

export const config: BytecodeStepperConfig = {
  explorationId: 'eip-7843',
  defaultExample: 'slotnum',
  gasLimit: 1_000_000n,
  maxStackDisplay: 8,
}
