/** Stable paths under `community-token/public/` — copied to dist on build. */
export const SITE_LOGO = {
  src: '/logo.png',
  alt: 'Feel Your Protocol community token logo',
  width: 512,
  height: 512,
} as const

export const SITE_FAVICON = '/favicon.png' as const
